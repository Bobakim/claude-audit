# 커밋된 텍스트 파일의 개행 문자를 항상 LF로 고정합니다.
# Windows의 core.autocrlf=true 설정과 관계없이 .prettierrc의 endOfLine: "lf"와
# 일치시켜, `npm run format:check`가 줄바꿈 문자 차이만으로 실패하는 것을 막습니다.
* text=auto eol=lf

*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.ico binary
*.woff binary
*.woff2 binary
